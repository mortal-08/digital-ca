{
  "name": "digital-ca-platform",
  "version": "1.0.0",
  "description": "Smart Digital Platform for Chartered Accountants",
  "scripts": {
    "start": "cd backend && node index.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm run dev",
    "client": "cd frontend && npm run dev",
    "build": "cd backend && npm install --production && cd ../frontend && npm install && npm run build",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
